<%= form_with model: @interviewer, local: true do |f|%>
    
    <%= render 'form', f: f %>

    <%= f.submit "Update Account" %>
<% end %>


<%= link_to 'Delete Account', interviewer_path(@interviewer), method: :delete %>